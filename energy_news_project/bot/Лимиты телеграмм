Да, Telegram имеет несколько **лимитов**, о которых важно знать при работе с ботами и каналами:

---

### 1️⃣ Сообщения

* **Максимальная длина текста**: **4096 символов** для одного сообщения.

  * Если текст больше — нужно делить на несколько сообщений.
* **Максимальная длина подписи ссылки/Markdown**: также ограничена 4096 символами, но если используешь Markdown или HTML — некоторые спецсимволы могут “съедать” часть лимита.
* **Количество ссылок, кнопок и медиа**:

  * **Inline-кнопки**: до **100 кнопок** в одном сообщении.
  * **Ряд кнопок**: до **8 кнопок в ряду**.

---

### 2️⃣ Отправка сообщений ботом

* **Скорость отправки** (rate limits):

  * **1 сообщение на чат**: обычно безопасно **1–2 сообщения в секунду**.
  * При превышении — может выдать `429 Too Many Requests`.
  * Для публичных каналов лучше делать **1 сообщение каждые 1–2 секунды**, а для групп — чуть медленнее.
* **Общее количество сообщений в минуту**: зависит от типа бота и канала. Для обычного бота Telegram рекомендует **не больше 30 сообщений в секунду на все чаты** (для каналов и групп).

---

### 3️⃣ Файлы и медиа

* **Фото, видео, документы**:

  * Максимальный размер: **50 МБ** для обычного бота (Telegram API).
  * Для Telegram Desktop и мобильных клиентов — до 2 ГБ, но через бота ограничения API 50 МБ.
* **Аудио и голосовые**: до 50 МБ.

---

### 4️⃣ Кнопки, inline-режим, callback

* **Callback data** (для InlineKeyboardButton): **64 байта** максимум.
* **Количество callback кнопок в сообщении**: до **100 кнопок**, как уже говорил.
* **Количество строк inline-кнопок**: примерно до 20–30, лучше не нагружать слишком сильно.

---

### 5️⃣ Чаты и участники

* **Максимальное количество участников в супергруппе**: 200 000.
* **Максимальное количество участников канала**: 200 000 публичных, неограниченно приватных (раньше было 200 000, сейчас почти без ограничений).

---

💡 **Практический совет для бота новостей**:

* Разбивай длинные новости на **несколько сообщений** по 3800–4000 символов.
* Держи **паузы 1–2 секунды между сообщениями**.
* Ограничь **количество кнопок на сообщение до 6–8**, чтобы не перегружать интерфейс.

---

Если хочешь, я могу прямо сейчас написать **готовый шаблон функции**, которая автоматически разбивает новость на сообщения по 4000 символов и вставляет кнопки корректно — чтобы больше никогда не ловить 429 или обрезку текста.

Хочешь, чтобы я так сделал?
